version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    build:
      html:
        - mkdir -p $READTHEDOCS_OUTPUT/html
        - echo "<html><body>scikit-verify docs placeholder</body></html>" > $READTHEDOCS_OUTPUT/html/index.html

python:
  install:
    - method: pip
      path: .
